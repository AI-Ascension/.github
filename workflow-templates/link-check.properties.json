{
  "name": "Link check",
  "description": "Checks simple inline Markdown destinations, single-line reference definitions, and HTTP(S) URLs. Supports angle-bracket destinations; relative files must exist and URLs must answer. Does not validate fragments or implement the full Markdown grammar. Uses a pinned checkout action and a shell step.",
  "categories": ["Documentation"],
  "filePatterns": ["README.md", "*.md", "docs/**/*.md"]
}

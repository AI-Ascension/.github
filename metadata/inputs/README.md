# Supplied specification

`2026-09-07-tagging-specification.md` preserves the operator-supplied tagging implementation prompt. Its inline section 4 topic sets and section 6 label mappings supply the previously missing proposal data. Separate duplicate proposal YAML files are unnecessary.

SHA-256: `e5c1d767add6fd16ffa3dd2e42f35209a442864e2dcb48695858ebc9c99cca68`.

This is a historical design input, not approved live desired state or authorization. Maintained topic desired state is `../repositories.yml`; shared definitions remain `/labels.yml`; migration rules are `../label-migrations.yml`. Changes to those files require review and the remote-write authorization described in the specification.
